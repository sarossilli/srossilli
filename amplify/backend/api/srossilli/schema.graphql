type Post 
  @model
  @auth(rules: [{allow: public}]) {
  id: ID! @primaryKey(sortKeyFields: "createdAt")
  title: String!
  shortDescription: String!
  banner: AWSURL!
  content: String
  createdAt: String!
}  