type Post 
  @model
  @auth(rules: [{allow: public}]) {
  id: ID!
  @index(
    name: "postsByDate"
    queryField: "postsByDate"
    sortKeyFields: ["createdAt"]
  ) 
  title: String!
  shortDescription: String!
  banner: AWSURL!
  content: String
  createdAt: String!
}  